import { ExamesAprovPipe } from './exames-aprov.pipe';

describe('ExamesAprovPipe', () => {
  it('create an instance', () => {
    const pipe = new ExamesAprovPipe();
    expect(pipe).toBeTruthy();
  });
});
